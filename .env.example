# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# =============================================================================
# Playwright Test Credentials
# =============================================================================
# These credentials are used by Playwright E2E tests to authenticate.
# Create test users in your Supabase project with the appropriate roles.

# Legacy user (used for general tests - any role works)
TEST_USER_EMAIL=test-user@yourcompany.com
TEST_USER_PASSWORD=your_secure_password

# SysAdmin user (sys_admin role - has access to /admin portal)
# If not set, falls back to TEST_USER_EMAIL/PASSWORD
TEST_ADMIN_EMAIL=test-sysadmin@yourcompany.com
TEST_ADMIN_PASSWORD=your_secure_password

# Standard Tenant User (org_user role - should NOT have access to /admin portal)
# Used for security isolation tests
TEST_TENANT_EMAIL=test-tenant-user@yourcompany.com
TEST_TENANT_PASSWORD=your_secure_password

# =============================================================================
# Security Test Requirements
# =============================================================================
# The multi-role security test suite requires TWO distinct test users:
#
# 1. TEST_ADMIN_* - A user with 'sys_admin' role in user_profiles
#    - Expected to access: /admin, /admin/tenants, /admin/users
#    - Create via: Supabase dashboard > user_profiles > set role = 'sys_admin'
#
# 2. TEST_TENANT_* - A user with 'org_user' role in user_profiles
#    - Expected to access: /dashboard, /projects, /clients, etc.
#    - Should be BLOCKED from: /admin/* routes
#    - Must belong to a tenant via tenant_users table
#
# If security tests fail with "tenant user gained access to admin portal",
# this indicates an RLS policy violation and MUST be fixed before deployment.
